"use-strict";

//new Gaming.GameSelector().show();

//Minesweeper.initialize();
FiresOfEmbora.initialize();

// (function() {
//     var buttons = Array.from(document.querySelectorAll("main > section").values()).map(function (section) {
//         return { label: section.querySelector("h1").innerText, action: null };
//     });
//     new Gaming.Prompt({
//         title: "Choose a Game",
//         unprioritizeButtons: true,
//         requireSelection: true,
//         buttons: buttons
//     }).show();
// })();
